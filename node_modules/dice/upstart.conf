# Install:
#
# sudo cp upstart.conf /etc/init/dice.conf
# sudo start dice
#
# Remove:
#
# sudo rm /etc/init/dice.conf

description "dice.js server"
author "Andrew Pennebaker <andrew.pennebaker@gmail.com>"

start on startup
stop on shutdown

script
	export HOME="/root"
	exec node /srv/d_hactar/usr/src/node-dice/dice.js -s 2>&1
end script
