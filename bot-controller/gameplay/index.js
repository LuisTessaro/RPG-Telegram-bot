// const { grindSpots, mainMenu } = require('../../menus/menus')
// const { startGrind } = require('./grind/grindFunctions')
// const { adventure, cast } = require('./adventure')

// const restMiddleware = require('../../middlewares/restMiddleware')

// const rest = require('./rest')

module.exports = (bot) => {
  // bot.command('grindSpots', ctx => ctx.reply('Pick a grind spot', grindSpots))

  // bot.command('grind', startGrind)

  // bot.command('rest', rest)

  // bot.use(restMiddleware)

  // bot.command('adventure', adventure)
  // bot.command('cast', cast)

  // bot.command('surrender', async ctx => {
  //   ctx.session.monster = {}
  //   ctx.session.playerInstance = {}
  //   ctx.session.isOnAdventure = false
  //   ctx.session.playerTurn = false
  //   return await ctx.reply('You surrendered!', mainMenu)
  // })
}
